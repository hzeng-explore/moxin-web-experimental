---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Magic Radio - Moxin AI">
  <div class="pt-32 pb-20 px-6 max-w-7xl mx-auto text-center">
    <div class="inline-block px-4 py-1 rounded-full bg-pink-50 text-accent-pink text-sm font-bold mb-6 animate-pulse">
      LIVE DEMO
    </div>
    <h1 class="text-5xl font-bold mb-8 text-text-primary">Magic Radio</h1>
    <p class="text-xl text-text-secondary max-w-2xl mx-auto mb-12 font-light">
      Listen to two AI agents debate any topic in real-time. Powered by Moxin LLM and local TTS.
    </p>

    <div class="max-w-2xl mx-auto bg-white border border-gray-200 rounded-3xl p-8 shadow-2xl relative overflow-hidden">
      <!-- Radio Visual -->
      <div class="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-accent-blue via-accent-purple to-accent-pink"></div>
      
      <div class="flex justify-center items-center gap-12 mb-12 mt-8">
        <div class="text-center">
          <div class="w-24 h-24 rounded-full bg-blue-50 border-2 border-accent-blue flex items-center justify-center mb-4 relative">
            <div class="absolute inset-0 rounded-full border border-accent-blue/50 animate-ping"></div>
            <span class="text-3xl">🤖</span>
          </div>
          <div class="font-bold text-accent-blue">Agent Alpha</div>
          <div class="text-xs text-text-secondary">Pro-Argument</div>
        </div>

        <div class="h-12 w-px bg-gray-200"></div>

        <div class="text-center">
          <div class="w-24 h-24 rounded-full bg-pink-50 border-2 border-accent-pink flex items-center justify-center mb-4 relative">
             <div class="absolute inset-0 rounded-full border border-accent-pink/50 animate-ping" style="animation-delay: 0.5s"></div>
            <span class="text-3xl">👩‍🔬</span>
          </div>
          <div class="font-bold text-accent-pink">Agent Beta</div>
          <div class="text-xs text-text-secondary">Con-Argument</div>
        </div>
      </div>

      <!-- Controls -->
      <div class="space-y-6">
        <input 
          type="text" 
          placeholder="Enter a topic (e.g., 'Is AI conscious?')" 
          class="w-full bg-gray-50 border border-gray-200 rounded-xl px-6 py-4 text-text-primary placeholder-gray-400 focus:outline-none focus:border-accent-purple focus:ring-2 focus:ring-accent-purple/20 transition-all"
        />
        <button class="w-full bg-gradient-to-r from-accent-blue to-accent-purple text-white font-bold py-4 rounded-xl hover:opacity-90 transition-opacity flex items-center justify-center gap-2 shadow-lg shadow-blue-500/20">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.752 11.168l-3.197-2.132A1 1 0 0010 9.87v4.263a1 1 0 001.555.832l3.197-2.132a1 1 0 000-1.664z"></path><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
          Start Broadcast
        </button>
      </div>

      <div class="mt-8 text-xs text-text-secondary">
        * This is a frontend demo. Backend connection requires local server instance.
      </div>
    </div>
  </div>
</Layout>
